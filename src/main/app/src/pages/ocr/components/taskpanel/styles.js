export const styles = (theme) => ({
    root       : {
        padding: theme.spacing(3.5),
    },
    formControl: {
        margin: theme.spacing(2),
    },
    runButton  : {
        color: 'white',
    },
    buttonIcon : {
        marginLeft: theme.spacing(1),
    }
})
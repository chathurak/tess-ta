export const styles = (theme) => ({
    documentPickerRoot: {
        padding      : theme.spacing(2),
        paddingBottom: 0
    },
    taskPickerRoot    : {
        padding   : theme.spacing(2),
        paddingTop: 0
    },
    datetimepicker    : {
        width: '180px',

        margin: theme.spacing(1.5),
    },
    select            : {
        width: '300px',

        padding: theme.spacing(1.5),
    }
})